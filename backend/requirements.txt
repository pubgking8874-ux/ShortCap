# ShortsCap backend — Phase 3 environment (database foundation).
# Only the packages actually installed and required for this phase are listed.
# Future phases add auth, Google OAuth, Alembic migrations, engines, etc. one at a time.

fastapi
uvicorn
pydantic-settings
SQLAlchemy
PyMySQL